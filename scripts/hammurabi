#!/usr/bin/env bash
# Launch the Hammurabi dashboard from anywhere, using the project venv.
set -euo pipefail
HERE="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec "$HERE/.venv/bin/python" "$HERE/src/dashboard.py" "$@"
